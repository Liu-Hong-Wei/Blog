# 使用官方 Node.js 镜像作为基础镜像
FROM node:20-alpine

# 设置工作目录
WORKDIR /app

# 复制 package.json 和 package-lock.json (如果存在)
COPY package*.json ./

# 安装项目依赖
RUN npm install

# 复制项目文件
COPY . .

# 暴露 Vite 开发服务器端口
EXPOSE 5173

# 运行开发服务器
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
